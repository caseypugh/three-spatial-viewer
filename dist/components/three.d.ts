/**
 * A small wrapper for THREE imports so rollup tree-shakes only the parts we need better
 */
export { Object3D, ShaderMaterial, Mesh, PlaneBufferGeometry, Texture, FrontSide, RGBFormat, RepeatWrapping } from 'three';
